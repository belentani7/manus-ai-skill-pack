node_modules/
*.log
# local agent notes — never part of the release
CLAUDE.md
.DS_Store
Thumbs.db
# playwright / QA artifacts
test-results/
playwright-report/
_*.mjs
**/shots/
**/*-capture.png
# recon artifacts (references and moodboards are other people's work — working material only)
design/refs/
design/moodboard/
assets/sourced/
# systemscan output: a report and one tile per rendered variant, regenerated by every run
design/system/

# OMC session state — operational artifacts, never part of the release
.omc/
!.omc/skills/

# recon working material — other people's imagery, never published
**/design/refs/shots/
**/design/moodboard/img/
**/design/moodboard/contact-sheet*
**/design/moodboard/_sheet*.html
